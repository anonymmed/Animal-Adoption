{% extends 'TemplateBundle:Layout:frontpage.html.twig' %}

{% block  content%}

    <center><h1>Liste Des Reservations</h1></center><br><br>
    <div class="box-body">
    <style type="text/css">
        table{border-collapse: collapse;width: 40%;font-size: 10pt;} table,th,td{border:1px solid #cfbfa6;} th{background:#cfbfa6;color: white;}
    </style>
    <table id="example2" class="table table-bordered table-hover">

                        <th>Nom petsitter</th>
                        <th>Adresse petsitter</th>
                        <th>tel petsitter</th>
                        <th>Date Debut</th>
                        <th>Date Debut</th>
                         <th>Supprimer</th>

                        </thead>
                        <tbody>
                        {% for r in reservations  %}
                                <td>{{  r.idPetsitter.nom}}</td>
                                <td>{{  r.idPetsitter.adresse}}</td>
                                <td>{{  r.idPetsitter.telephone}}</td>
                                <td>{{  r.dateDebut|date}}</td>
                                <td>{{  r.dateFin|date}}</td>

                            <td><a href="{{ path('annulerReservation',{'id':r.id}) }}" class='btn btn-default'>X</a></td>

                            </tr>
                        {% endfor %}

                        </tbody>
                    </table>

{% endblock %}