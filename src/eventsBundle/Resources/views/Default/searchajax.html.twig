

{% extends 'TemplateBundle:Layout:backpage.html.twig' %}

{% block  content %}
<div class="tableau" >
    <a href="{{ path('create') }}" class="btn btn-default"><span>Ajouter</span></a>

    <a href="{{ path('searchajax') }}" class="btn btn-default" ><span>Rechercher</span></a>
    <center><h1>Liste Des Concours</h1>
        <div class="box-body">
            <div class="box-body">
                <form>


                </form>
                <style type="text/css">
                    table{border-collapse: collapse;width: 40%;font-size: 10pt;} table,th,td{border:1px solid #cfbfa6;} th{background:#cfbfa6;color: white;}
                </style>
                <form>

                    <input type="text" name="description" id="descriptionid" placeholder="Rechercher">
                </form>

                    <table id="table" class="table table-bordered table-hover">
                        <thead>
                        <th>Id</th>
                        <th>description</th>
                        <th>nbre de places</th>
                        <th>date</th>

                        </thead>
                    <tbody id="tbody">

                    <table id="table" class="table table-bordered table-hover">
                        <style type="text/css">
                            table{border-collapse: collapse;width: 40%;font-size: 10pt;} table,th,td{border:1px solid #cfbfa6;} th{background:#cfbfa6;color: white;}
                        </style>

                    <script src="{{ asset('jquery-3.3.1.min.js') }}"></script>
                    <script>
                        $('#descriptionid').keyup(function () {
                            var input=$('#descriptionid').val();
                            $.ajax({
                                url:"{{ path('searchajax') }}",
                                type:'Post',
                                data:{'description':input},
                                success:function (data) {
                                    //console.log(data);
                                    $.each(data,function (i,e) {
                                        $('#table').append("<tr><td>"+e.id+"</td><td>"+e.description+"</td><td>"+e.nbredeplaces+"</td><td> "+e.date+"</td></tr>");
                                    })
                                }
                            })


                        });

                    </script>

                    </tbody>
                </table>

                {% endblock %}

