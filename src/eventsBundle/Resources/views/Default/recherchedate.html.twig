
{% extends 'TemplateBundle:Layout:backpage.html.twig' %}

{% block  content %}
<div class="tableau" >
    <a href="{{ path('create') }}" class="btn btn-default"><span>Ajouter</span></a>

    <a href="{{ path('searchajax') }}" class="btn btn-default"><span>Rechercher</span></a>
    <a href="{{ path('pdf') }}" class="btn btn-default"><span>Exportez en pdf</span></a>
    <a href="{{ path('graphePie') }}" class="btn btn-default"><span>Statistiques</span></a>
    <center><h1>Liste Des Concours prevues</h1>
        <div class="box-body">
            <div class="box-body">
                <form>


                </form>
                <style type="text/css">
                    table{border-collapse: collapse;width: 40%;font-size: 10pt;} table,th,td{border:1px solid #cfbfa6;} th{background:#cfbfa6;color: white;}
                </style>
                <table id="table" class="table table-bordered table-hover">
                    <thead>
                    <th>Id</th>
                    <th>description</th>
                    <th>nbre de places</th>
                    <th>date</th>
                    <th>Supprimer</th>
                    <th>Modifier</th>


                    </thead>
                    <tbody id="tbody">
                    {% for concour in concours  %}
                        <tr><td>{{concour.id }}</td>
                            <td>{{concour.description }}</td>
                            <td>   {{concour.nbredeplaces }}</td>
                            <td>{{concour.date | date }}</td>

                            <td><a href="{{ path('delete', {'id':concour.id}) }}" class='btn btn-default'>X</a></td>
                            <td><a href="{{ path('update',{'id':concour.id}) }}" class='btn btn-default'><i class='fa fa-pencil-square-o'></i></a> </td>

                        </tr>
                    {% endfor %}

                    </tbody>
                </table>

                {% endblock %}

